# Expo / React Native (managed workflow) — on top of the monorepo root .gitignore

# Expo
.expo/
dist/
web-build/
expo-env.d.ts.bak

# Native projects are generated by `expo prebuild` / EAS — not committed in the
# managed workflow. Delete these lines if you deliberately go bare.
/ios/
/android/

# Metro
.metro-health-check*

# Env
.env
.env.local
.env.*.local

# Debug / misc
npm-debug.*
*.orig.*
