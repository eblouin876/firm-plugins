{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//extends": "Expo's own base (from the expo package) sets the correct React Native module/JSX/resolution for Metro — module:preserve, moduleResolution:bundler, jsx:react-jsx, customConditions:[\"react-native\"]. We layer the monorepo's strictness on top rather than re-deriving the RN base.",
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": [
    "app",
    "src",
    "expo-env.d.ts",
    ".expo/types/**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
