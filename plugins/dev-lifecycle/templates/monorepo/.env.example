# Placeholders only — copy to .env and fill in real values locally.
# NEVER commit real secrets to this file or to .env.
#
# Populated incrementally as blocks are scaffolded in (backend, frontend,
# infra). Each block's co-located doc fragment documents the env vars it
# needs and where to get them (see the root README's Secrets section once
# docs:generate runs — Stage 4, #24).

# NODE_ENV=development
