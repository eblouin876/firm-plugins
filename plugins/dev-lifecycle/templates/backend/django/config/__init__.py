"""The Django project package: settings, root URLconf, ASGI/WSGI entrypoints.
Not vendored — standard Django project-package boilerplate, named `config`
per this block's own convention (not the Django default project name) so
`DJANGO_SETTINGS_MODULE=config.settings` reads clearly regardless of what a
materialized project ends up calling its top-level app package."""
